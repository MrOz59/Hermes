# Membership in this group is what the polkit rule keys on, and what marks an
# account as belonging to an isolated session rather than to a person. It is
# created here so the rule is never live without the group existing.
g hermes-session -
